# Cache directory

This directory is reserved for local cache files generated during experiment
reruns.

Large cache files are not tracked in the regular GitHub repository. The
experiment scripts use `fast_cache_theta/` by default for cached catalog and
theta-coefficient files.
