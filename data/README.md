# Data directory

This directory is reserved for generated benchmark data artifacts.

The manuscript tables and figures are regenerated from the result CSV files in
`results/`. Large generated numerical artifacts, such as theta-coefficient
matrices, are stored locally in `fast_cache_theta/` and may be included in the
Zenodo archive rather than the regular GitHub repository.
